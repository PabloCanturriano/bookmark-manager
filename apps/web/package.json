{
   "name": "@bookmark-manager/web",
   "version": "0.0.1",
   "private": true,
   "scripts": {
      "dev": "echo 'web dev — to be configured in phase 7'",
      "build": "echo 'web build — to be configured in phase 7'",
      "test": "echo 'web test — to be configured in phase 7'",
      "lint": "eslint src"
   },
   "devDependencies": {
      "@bookmark-manager/config": "workspace:*",
      "eslint": "^9.21.0",
      "typescript": "^5.7.3"
   }
}
